name: Notes

on:
  workflow_dispatch:

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Render deploy note
        run: |
          echo "Render can auto-deploy directly from GitHub after every push."
          echo "This workflow is only a placeholder so the repo has a deployment note."
